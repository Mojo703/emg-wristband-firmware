Open issue 1 â serial can't carry the EMG stream to the real dashboard. The confusing part: my pyserial test scripts sustain the exact same stream from the same firmware at ~20 KiB/s without a single stall, and small frames (hello, logs) get through to the dashboard fine. So it's something specific to how the dashboard's tokio-serial/serialport-rs session drains the port once bulk data starts. I ruled out DTR (asserting it didn't help), RTS (deasserting didn't help), the device's write timeout (100â500ms, still stalls), and the dashboard's reader design (blocking thread, 100ms poll â looks correct). Earlier runs streamed serial for ~11s before stalling; after the latest firmware flash it stalls near-instantly, and I haven't explained that shift either. I was mid-step instrumenting the backend reader with byte-rate counters to see whether it's reading at all during the stall when you stopped me â that measurement is the next diagnostic, and the instrumentation code is written but unbuilt.

Open issue 2 â a second, rarer hang mode still exists. The main crash (TCP thread stack overflow â silent panic-reboot loop) is fixed and verified. But the device hard-hung once more after sitting ~10 minutes in the dial-retry loop with the backend down â main stops draining the CDC entirely, and it doesn't self-recover. I couldn't capture a cause since there's no console. As a mitigation I armed the task watchdog on the main loop (CONFIG_ESP_TASK_WDT_PANIC + feed per iteration), so any future hang becomes a 5-second panic-reboot with the reason named in the boot log instead of a dead device. That firmware is flashed, but the hang hasn't recurred yet to prove the watchdog catches it.