# CLAUDE.md: ota-server

Minimal `axum` server that hosts firmware `.bin` images for `ota-client`. Plain host
Rust, single source file `src/main.rs`. See `README.md` for how to run it and the
firewall gotcha.

Nothing firmware-specific here; it builds and runs on this host.
