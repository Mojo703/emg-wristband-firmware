# Runtime dependencies for the pose inference service.
# The mock estimator only needs these.

websockets>=12.0
cbor2>=5.6.0
numpy>=1.26.0

# For the real Meta emg2pose model, also install:
#   pip install -r requirements-emg2pose.txt
#   python setup_emg2pose.py
