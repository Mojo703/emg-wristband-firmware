# Runtime dependencies for the pose inference service.
# The mock estimator only needs these; the real emg2pose integration would add
# torch, emg2pose, and UmeTrack from the Meta repository.

websockets>=12.0
cbor2>=5.6.0
numpy>=1.26.0
