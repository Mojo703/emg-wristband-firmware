# Single-app layout (4 MB flash). One large app partition + nvs (device settings)
# + one flash-resident calibration store.
#
# `training` keeps the v2 map: one shipped prior followed by two 192 KB physical
# slots. Slot sequence, role and CRC recover one logical resident plus one
# candidate or scratch without a separate selector region.
#
# 0xF0000 fills the rest of the 4 MB part exactly (0x310000 + 0xF0000 =
# 0x400000). `firmware-bench/FLASH-FORMATS.md` fixes every internal
# offset. `playback-host build-partition-v2` produces the separately flashed
# image.
#
# Name,     Type, SubType,   Offset,   Size,    Flags
nvs,        data, nvs,       0x9000,   0x6000,
phy_init,   data, phy,       0xf000,   0x1000,
factory,    app,  factory,   0x10000,  0x300000,
training,   data, undefined, 0x310000, 0xF0000,
