# Single-app layout (4 MB flash). One large app partition + nvs (device settings)
# + flash-resident calibration training rows.
#
# `training` holds the packed training rows a calibration fit joins with the
# rows it collected live. They live in flash because they do not fit anywhere
# else: the full training set is 9654 rows, which is 604 KB even at int8, well
# past SRAM. The fit reads them through a memory-mapped view rather than copying
# them in, so the cost is flash-cache bandwidth rather than heap.
#
# 0xF0000 fills the rest of the 4 MB part exactly (0x310000 + 0xF0000 =
# 0x400000) and holds ~14000 int8 rows. Written separately with
# `espflash write-bin`, not by the app; `firmware-bench/PROTOCOL.md` documents
# the image layout and `playback-host build-partition` produces one.
#
# Name,     Type, SubType,   Offset,   Size,    Flags
nvs,        data, nvs,       0x9000,   0x6000,
phy_init,   data, phy,       0xf000,   0x1000,
factory,    app,  factory,   0x10000,  0x300000,
training,   data, undefined, 0x310000, 0xF0000,
